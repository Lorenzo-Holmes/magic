# v3.0.1 云山玉阙：运行美术接入

## 本轮实际交付

用户保存的资源包已在 `D:\我欲飞升` 校验并导入。原包为 2,444,398 bytes，SHA-256 为 `66735b5f54192936ef4f1f74e80b4abe8fd5d7f4782e313db7c48e99f7126c68`。原 ZIP 保留且不加入 Git；84 个导入文件包含 82 个 WebP 和两份清单。清单内每个切片的来源、裁切、尺寸、透明通道和摘要保持不变。

运行时为现有 JavaScript 引擎加独立窗口模板。没有恢复原作 Lua，也不把文件名叫 Window 等同于运行 Unity Prefab。新图只提供表现，修为、资源、物品、随机数、revision 和两项存档键仍由原引擎管理。

## 已实装窗口

| 页面 | 实装内容 |
| --- | --- |
| 洞府 | 独立竖版背景，山门、丹器阁、问机台、灵兽居、静室五个透明物件；建筑图像就是按钮的一部分，不是旧圆章。共享画布变换，HUD、修为、状态命令与底栏分层。短屏使用单独适配。 |
| 人物 | 独立场景与生成道身立绘；四槽读取实际装备，点槽仅展示本槽候选，可鉴定、穿卸、温养、蜕变。弹窗刷新保留当前槽位，关闭恢复焦点。人物图不是实时换装模型。 |
| 行囊 | 真实五分类、装备容量、图标、数量、资源代价和独立滚动区。每件装备使用明确的 ID→类别图标映射，并非每件都有独占插画。 |
| 山海 | 704×1408 原创画卷，六地锚点、背景和标记共用同一镜头；支持拖动、按钮缩放、复位与全图/路线切换。标签反向缩放，触控区不随镜头缩小。 |
| 丹器 | 独立大殿、透明炉体和火焰，材料占独立布局行，不被配方覆盖。三种炼法和本命炼器沿用真实费用，结果先保存再显示。 |
| 突破与旧章节 | 突破使用独立背景、光环和 HTML 境界名，可略过；减少动态效果时仍显示结果。主线、行旅、仙界、创世与旧信息窗保留原模板和玩法，并接入共同纸纹、场景与边框，不宣称所有旧模板都重写为新窗口。 |

全局菜单入口和教学批注已恢复。图片加载失败时只隐藏失败图层，功能标签、按钮和存档继续可用。没有通过失败回调重新结算奖励或请求外站。

## 资源与发布边界

运行包包含 131 个文件，全部自包含，不携带字体、截图、原始大图、开发依赖或参考游戏资源。旧两幅横向插画仍留在源码历史中，但已从生产白名单退役；构建只清理 dist 中明确列出的旧副本，保留源文件与其他用户文件。未放宽 3,000,000-byte ZIP 门槛。

本轮调用的是已保存的生成素材；没有再次生成概念图。工具来源记录不额外声称已核验具体的 image2.5 模型标识。美术是否符合用户最终审美仍须用户亲自判断，userVisualApproval 保持 pending。

## 验收方式

全量 Node 测试、source/dist/ZIP 校验及浏览器主线验收分别执行并留证。V3 快速脚本增加所有 82 图解码、六地选点、缩放/拖动/复位只读、满 12 件行囊、具体槽位穿卸、炼丹真实扣料、以及独立上下文中的故意断图测试。

正常加载错误与故意断图分开记录。满行囊/炼丹的补充测试使用隔离 QA 存档，不是用户的浏览器存档，也不冒充自然游玩获得的装备。实体 iOS/Android 设备尚未测试。图片在游戏中的视觉检查使用真实浏览器截图，不用概念稿替代。

## 最终实测结果

| 检查 | 本轮结果 |
| --- | --- |
| 全量 Node | 195/195，退出码 0；`output/ui-v3-art-node-final.log` |
| 构建与包校验 | 均为退出码 0，源码、dist 和 ZIP 字节一致 |
| 完整浏览器 | 退出码 0，540 次布局、12 次弹窗布局，正常验收无控制台错误、失败请求或外部请求 |
| 主线与兼容 | 真实 DOM 点击完成凡界，并从所得存档继续仙界与创世；源码、dist、ZIP 解包后的 file 模式均在套件中验证 |
| 五窗口专项 | 33 组检查通过，含六种视口、34 px 模拟底部安全区、满行囊末尾与成丹场景 |
| 美术与镜头 | 82 图逐一解码，尺寸与清单一致；六地点击、镜头缩放/拖动/复位不改存档 |
| 物品与异常 | 隔离 12 件行囊夹具、按槽筛选/穿卸、炼丹扣 2 灵草及 1 灵露；故意令人物图加载失败后按钮与存档仍可用 |

完整浏览器证据：`output/playwright/v3.0.1/runs/acceptance-Z64ILA/`。
专项证据：`output/playwright/ui-v3/quick-Uw0JM9/`。
当前截图已实际打开检查洞府、人物、地图、丹器，以及满行囊滚动末尾和短屏洞府。

发布候选：`release/wo-yu-fei-sheng-v3.0.1.zip`；2,674,729 bytes，解压文件合计 3,129,467 bytes，131 个运行文件。
SHA-256：`fb26675102848c824b1cc0fd6b9c1b8bf45fd1e79914fe38f2dedd2f598fca81`。

最初构建发现旧 dist 插画残留，已通过明确退役清单清理构建副本。镜头画布允许在自己的裁切区之外延伸，页面/HUD 溢出与控件命中仍单独检查；顶层原生弹窗的验收选择器限定在活动弹窗内，避免误选背景中不可交互的同名按钮。最终完整套件重新运行通过，没有引用改动前 ZIP 的结果。

预览分支仍为 `preview/ui-v3-functional-20260910`。本机预览为 `http://127.0.0.1:4317`，也可双击源码或 dist 的 index.html。用户视觉批准保持 pending，本轮不覆盖 main。
