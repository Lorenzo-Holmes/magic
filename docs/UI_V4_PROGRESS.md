# UI V4 执行进度：共享界面基础批次

原任务：《我欲飞升_前端实施清单_UIV4_v1.1.md》。原文件 SHA-256：`4aff156c77a0137e1b1febcdc4b1ddb7f69dd7c7b6cd03f200e8d49718b633d3`。

本批次从 `14afd1c` 建立 `feat/ui-v4-unified`，候选显示版本为 `3.0.3`。这是 UI-00～03 的基础候选，不是 18 项全部完成或已经发布的 V4。

## 实际实现

UI-00 已保存基线、页面/状态矩阵和合法行动生成的固定样本，检查点为 `b7b2899`。既有未跟踪文件 `docs/CULTIVATION_REDESIGN_V4.md` 保留，不纳入本任务自动提交。

UI-01 已接入 light/dark 语义颜色，仙界标题、说明、按钮、费用和手记不再继承浅纸色文字。禁用按钮保留不透明文字，以颜色、底面和虚线区分；不再把整块内容降低到约 0.48 透明度。纸面弹窗具有自身主题，不继承修行深色舞台的浅字。

UI-02 已创建四类面板、四级按钮、字体与状态样式，并接到原 button()。action、id、kind、revision 与可访问名称保持原入口；地图节点、数字详情、装备槽与修炼圆主按钮不被改成通用矩形。组件展示页包含深浅主题、长标题、费用、不可用状态和危险操作，仅用于开发测试。

UI-03 已把两条游戏外壳合并到 route-model / shell；旧 Workbench 导航与 V3 HUD 均委托同一 navigation.js。凡界、仙界、创世使用相同暗玉底栏与原四个导航 ID。导航是独立 HTML 按钮，不是一张烘焙了选中态的图片。原字形导航不再绘制，修行独有的灰度底栏覆盖已删除。

长信息页现在在 540px 舞台内独立滚动，底栏在滚动容器外占位。仙界手记读取仙界 journal；创世读取世界 history；凡界回看读取原 log。通过界面上下文区分“返回当前修行”与“回看凡界”，切换功能页不丢失创世或仙界上下文。

仙界操作区已经变为一个主操作和紧凑辅助工作台，文字可以阅读。但独立角色场景与仙界完整呈现迁移属于 UI-11，当前不得登记 UI-11 完成。

## 已完成的专项检查

改造前：`output/ui-v4-foundation/before-nmrop1/`，13 张截图。

基础层第一次完整专项证据：`output/ui-v4-foundation/foundation-r55Icc/`，24 张截图、22 个布局记录。包含普通修行、五槽进化、创世的六种视口，其余四窗口、组件展示和仙界操作区。

仙界抽样 36 个文字/控件与最终不透明表面的计算对比度通过，最小为 4.655:1；还检查控件整体透明度。该数值不代表每幅带图像、渐变或透明背景的区域都经过逐像素检测。场景文字的其它区域将在后续逐页验收继续扩大覆盖。

另验证 21 个滚动到可见位置后的控件中心/内缩边缘命中；三个章节往返的原存档字符串不变；仙界缓炼的 DOM 操作结果与原引擎一致；创世返回、凡界回看、行旅占用及关闭弹窗返回焦点通过。组件展示另有 38 项检查。

全量 Node 本轮已通过 215/215；行旅仿真通过 1200 个合法行动样本。日志位于 `output/ui-v4-foundation/verification-LIPflo/`。

## 保留的失败与验证边界

首次色板测试发现部分初始配色在凹槽底面不足 4.5:1，已加深对应文字，未降低门槛。详见 `UI_V4_CSS_MIGRATION.md`。

完整回归在 final-simulation 的版本门禁失败：该旧脚本仅接受 major 1/2，而当前为 3.0.3。已增加 major 3 并补充 package/scenes 版本一致性断言；6000 个样本、原六条路线、业务断言和战力差异阈值均未减少。原失败报告保留；后续从此步骤显式继续，不把旧报告改成通过。

修复门禁后的完整 6000 样本仿真、构建、包校验和同包浏览器回归均以退出码 0 完成。原失败记录保留为 `verification-LIPflo`，显式续接记录位于 `output/ui-v4-foundation/verification-tail-gyUWwz/`；后者记录前置报告摘要与续接期间的生产源码哈希，未将旧失败改成通过。

## 最终基础批次技术验收

| 项目 | 实际结果 |
| --- | --- |
| 全量 Node | 215/215；退出码 0 |
| 行旅仿真 | 1200 个合法行动样本；退出码 0 |
| 经典兼容压力仿真 | 六路线 ×1000，6000/6000 凡界飞升及仙界序章完成；均值战力差异比 1.182445，原阈值仍为 4 |
| 构建 / 包验证 / 浏览器 | 退出码均为 0 |
| 同包完整浏览器 | 540 次布局、12 次弹窗布局检查；主线完成；正常用例控制台错误、失败请求、外部请求均 0 |
| 最终源码专项 | `output/ui-v4-foundation/foundation-1VyQa3/`，24 截图、22 布局、36 文字对比度、21 控件命中、38 组件样例检查通过 |
| 实体设备 | 尚未测试；安全区等为浏览器模拟 |
| 用户视觉 | pending；没有把技术通过当成用户认可 |

完整浏览器证据：`output/playwright/v3.0.3/runs/acceptance-U8sGS7/`。正式同包测试 SHA-256 与构建报告及最终源码一致。

本地包：`release/wo-yu-fei-sheng-v3.0.3.zip`。
ZIP **2,977,447 bytes**，原始生产文件 **3,453,912 bytes**，**145** 个生产文件。
SHA-256：`1381dcd1d1a41b53e33e9912d9e24de4061c6fcd29c7730b3c266187325b1957`。

剩余 ZIP 预算仅约 22.5 KB；新人物包尚未进入生产。下一批必须按清单清理与压缩重复素材后再测，不得直接加图然后放宽 3MB 门槛。145 文件来自原 133 项加 12 个共享模块/样式，生成脚本、测试、文档和人物原始 PNG 不在包中。

复查本批全部技术步骤用 `node tools/run-ui-v4-foundation.cjs`。一次性的续接脚本归档在 tail 证据目录，不作为通用执行工具。源码专项使用 `FS_UI_BASE_URL=http://127.0.0.1:4318`；当前 4318 服务已加载新生产清单，不能把另一个旧 4317 进程当成新版验证。

## UI-04：已准备，等待实际素材传输

本机目前仍没有 `assets/ui-v4` 或 `wo-yu-fei-sheng-ui-v4-characters.zip`。已从会话中存在的四张角色生成原图制作四张站姿 WebP 与四张头像；视觉查看了站姿/头像联系表，未代填用户认可。包内没有四张配对坐姿。

资源包：`wo-yu-fei-sheng-ui-v4-characters.zip`，489,937 bytes。
SHA-256：`972ae11e7cf9dbd76b928f83e7f31d04fc854b99486fa4aec7844da20510964a`。交付 ZIP 采用固定条目时间戳以便重复构建；以该最终交付摘要为准。

用户保存到 `D:\我欲飞升\wo-yu-fei-sheng-ui-v4-characters.zip` 后，可由下一执行轮先执行：

```text
node tools/import-ui-v4-art.cjs --check
node tools/import-ui-v4-art.cjs
```

实际脚本使用位置参数，不存在 --zip 参数。新导入器独立于固定 82 项的 V3 导入器，校验 ZIP 路径、CRC、SHA-256、资源清单及静态 manifest.js 内容；拒绝覆盖不同的已有素材，支持相同包幂等导入。导入不更改存档、生产白名单或人物选择。

UI-04 的“新旧清单合并、生产预算、页面接入”尚未完成。四名角色选择与坐姿联动尚未实现；不能因新增导入器或制作了 ZIP 就把 UI-05/06 记为完成。新增包也尚未计算进入最终生产体积，下一步必须先清理/压缩重复资源，不能把 3MB 上限静默放宽。

## 发布边界

本轮不推送 GitHub、不合并 main、不部署 Cloudflare。既有游戏引擎及两个存档键不改名、不清空。没有创建 DevSpace Goal，不填写 goalRef。全批次视觉状态保持 pending。
